#Jenkinsfile
echo "Nathan's Java Pipeline: START
pipeline {
    agent { docker { image 'maven:3.3.3' } }
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
            }
        }
    }
  
  
  
}
